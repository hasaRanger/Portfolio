/**
 * Semantic Versioning (SemVer)
 * Format: MAJOR.MINOR.PATCH
 * 
 * MAJOR - Increment when you make incompatible API changes
 *         Example: 1.0.0 → 2.0.0 (removing features, breaking changes)
 * 
 * MINOR - Increment when you add functionality in a backward-compatible manner
 *         Example: 1.0.0 → 1.1.0 (new features, no breaking changes)
 * 
 * PATCH - Increment when you make backward-compatible bug fixes
 *         Example: 1.0.0 → 1.0.1 (bug fixes, patches)
 * 
 * Examples:
 * - 0.1.0 (initial development) → 1.0.0 (first stable release)
 * - 1.2.3 (add new component) → 1.3.0 (minor version bump)
 * - 1.2.3 (fix typo in docs) → 1.2.4 (patch version bump)
 * - 1.2.3 (restructure API) → 2.0.0 (major version bump)
 */

// 2026-05-10 | 12.46 P.M. | Update resume link to direct Google Drive download in MobileNav and Sidebar components
export const SITE_VERSION = "1.0.6";